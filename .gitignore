# Python
.venv/
__pycache__/
*.pyc

# Secrets
.env

# Generated app output — reproducible by running the agent, not versioned
generated-app/

# Boilerplate is committed (the agent needs it to scaffold --output; a
# fresh clone must be runnable without a separate download) — only ignore
# what npm would generate inside it if someone installs there directly.
/Fullstack-Coding-Challenge-main/node_modules/
/Fullstack-Coding-Challenge-main/dist/

# Assessment PDF is reference material, not part of the deliverable
/*.pdf
